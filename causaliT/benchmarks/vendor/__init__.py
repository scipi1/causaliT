"""
Verbatim third-party source, vendored only where no package exists.

Each subpackage carries its upstream ``LICENSE`` and a ``PROVENANCE.md`` naming
the source repository, the files copied and every local modification.  Vendoring
is a last resort: DAGMA and PC come from their maintained PyPI packages
(``dagma``, ``causal-learn``) and are therefore *not* vendored.

Current contents:

``notears/``  NOTEARS solvers (Apache-2.0); upstream has no PyPI release.
"""
